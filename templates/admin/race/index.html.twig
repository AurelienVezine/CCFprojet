{% extends 'admin/admin.html.twig' %}

{% block title %}Admin - Les Breeds{% endblock %}

{% block main %}
    <h1>Les Breeds</h1>
    <p>
        <a class="btn btn-dark" href="{{ path('admin.race.create') }}">Ajouter une breed</a>
    </p>
    <table class="table">
        <thead>
        <tr>
            <th> Nom</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for race in races %}
            <tr>
                <td style="width: 300px">{{race.label}}</td>
                <td>
                    <div class="d-flex gap-1">
                        <a class="btn btn-primary btn-sm" href="{{ path('admin.race.edit', {id: race.id})}}"><i class="bi bi-pen p-1"></i></a>
                        <form action="{{ path('admin.race.delete', {id: race.id})}}" method="post">
                            <input type="hidden" name="_method" value="DELETE">
                            <button type="submit" class="btn btn-danger btn-sm"><i class="bi bi-trash p-1"></i></button>
                        </form>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}