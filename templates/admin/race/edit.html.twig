{% extends 'admin/admin.html.twig' %}

{% block title %}Admin - Edition d'une race{% endblock %}

{% block body %}
    <h1>Editer la race</h1>
    {{ form(form) }}
    <a class="btn btn-danger btn-sm" href="{{ path('admin.race.index')}}">Retour</a>
{% endblock %}